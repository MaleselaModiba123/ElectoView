# TEST_CASES.md — ElectroView: Electircity Usage Analytics dashboard

## 1. Functional Test Cases
 
| Test Case ID | Requirement ID | Use Case | Description | Preconditions | Test Steps | Expected Result | Actual Result | Status |
|---|---|---|---|---|---|---|---|---|
| TC-001 | FR-01 | UC-01 | Successful login with valid credentials | A registered user account exists with role "Analyst." The system is running. | 1. Navigate to `/login`. 2. Enter valid email and password. 3. Click "Sign In." | User is authenticated within 2 seconds. A JWT is issued. User is redirected to the Analyst dashboard. | — | — |
| TC-002 | FR-01 | UC-01 | Login fails after 5 consecutive incorrect passwords | A registered active user account exists. | 1. Navigate to `/login`. 2. Enter correct email and incorrect password. 3. Repeat step 2 five times. | After the 5th failure, the system displays "Your account has been locked. Contact your administrator." No JWT is issued. Subsequent correct login attempts are rejected until an admin unlocks the account. | — | — |
| TC-003 | FR-01 | UC-01 | Consumer cannot access admin endpoint | A Consumer-role JWT exists. | 1. Authenticate as a Consumer user and obtain JWT. 2. Make a GET request to `/api/admin/users` with the Consumer JWT in the Authorization header. | System returns HTTP 403 Forbidden with message "Insufficient permissions." | — | — |
| TC-004 | FR-03 | UC-02 | Zone dashboard displays all active zones | At least 3 active zones with meter readings in the last 24 hours exist in the database. Authenticated as Electrcity Distribution Manager. | 1. Log in as Electrcity Distribution Manager. 2. Navigate to the Zone Dashboard. 3. Observe the rendered zone table. | All active zones are displayed. Each row shows: zone name, total kWh today, load percentage, and a colour-coded status indicator. Dashboard renders within 2 seconds. | — | — |
| TC-005 | FR-05 | UC-04 | Anomaly detected when reading exceeds zone threshold | Zone C has a threshold set to 180 kWh per reading. An active meter exists in Zone C. | 1. Submit a POST request to `/api/meters/readings` with `{ meterId: "KC-0118", kwhConsumed: 210, recordedAt: <now> }`. 2. Wait up to 10 seconds. 3. Query `/api/anomalies` filtered to meter KC-0118. | An anomaly record exists with status "Open," value 210, threshold 180, and a timestamp within 10 seconds of submission. An in-app notification appears in the admin panel. An alert email is dispatched to the zone administrator. | — | — |
| TC-006 | FR-05 | UC-04 | No anomaly created when reading is within threshold | Zone A has a threshold set to 250 kWh. An active meter exists in Zone A. | 1. Submit a POST request to `/api/meters/readings` with `{ meterId: "BV-0207", kwhConsumed: 200, recordedAt: <now> }`. 2. Wait 10 seconds. 3. Query `/api/anomalies` filtered to meter BV-0207 for the current timestamp. | No new anomaly record is created for this reading. No notification is generated. | — | — |
| TC-007 | FR-06 | UC-05 | Electrictiy Technician can update anomaly status to Resolved | An anomaly record with status "Open" exists. User is authenticated as Electrictiy Technician. | 1. Log in as Electrictiy Technician. 2. Navigate to the Anomaly Log. 3. Select the open anomaly. 4. Change status to "Resolved." 5. Enter resolution note: "Meter replaced on site." 6. Click Save. | Anomaly record status is updated to "Resolved." Resolution note is saved. A log entry records the technician's user ID, timestamp, and status transition (Open → Resolved). | — | — |
| TC-008 | FR-07 | UC-06 | Consumer views personal usage for current billing period | A consumer account is linked to meter MP-0055 with readings for the current and previous billing months. | 1. Log in as the Consumer user. 2. Observe the personal dashboard. | A bar chart displays daily kWh consumption for the current billing period. The previous period is overlaid for comparison. Total kWh for each period is displayed. A percentage change indicator is shown (e.g., "↑ 8% vs last month"). | — | — |
| TC-009 | FR-07 | UC-06 | Consumer cannot access another meter's data | Consumer A is linked to meter MP-0055. Consumer B is linked to meter BV-0201. Consumer A is authenticated. | 1. Log in as Consumer A. 2. Make a GET request to `/api/meters/BV-0201/readings` using Consumer A's JWT. | System returns HTTP 403 Forbidden. Consumer A's dashboard shows only data for meter MP-0055. | — | — |
| TC-010 | FR-08 | UC-07 | Analyst generates a CSV report for a 30-day period | Readings exist for Zone A for the past 30 days. User is authenticated as Analyst. | 1. Log in as Analyst. 2. Navigate to Reports. 3. Set start date to 30 days ago, end date to today. 4. Select Zone A. 5. Select CSV format. 6. Click "Generate Report." 7. Download the file. | A CSV file is downloaded within 10 seconds. The file contains columns: `meter_id`, `zone`, `date`, `kwh_consumed`, `anomaly_flag`. All rows correspond to Zone A meters in the selected date range. | — | — |
| TC-011 | FR-09 | UC-08 | Admin creates a new Analyst user account | User is authenticated as Administrator. No account exists for `newanalyst@electrcoview.co.za`. | 1. Log in as Administrator. 2. Navigate to User Management. 3. Click "Add User." 4. Enter name, email `newanalyst@electrcoview.co.za`, role "Analyst." 5. Click Save. | New user account is created with status "Active" and role "Analyst." A welcome email with a temporary password is sent to the provided email address. Creation event appears in the audit log. | — | — |
| TC-012 | FR-12 | UC-11 | Bulk CSV import with mixed valid and invalid rows | User is authenticated as Analyst. A CSV file exists containing 100 valid rows and 5 rows with missing `kwh_consumed` values. | 1. Log in as Analyst. 2. Navigate to Data Import. 3. Upload the CSV file. 4. Observe the import progress and summary. | The system imports 100 valid rows successfully. The 5 invalid rows are rejected with per-row error messages indicating the missing field. The import summary displays: "100 imported, 5 skipped." The system does not fail the entire import due to the invalid rows. | — | — |
 
---
 
## 2. Non-Functional Test Cases
 
### 2.1 Performance Test Cases
 
| Test Case ID | Requirement ID | Description | Test Type | Tool | Steps | Expected Result | Actual Result | Status |
|---|---|---|---|---|---|---|---|---|
| TC-NFR-P01 | NFR-P01 | Zone dashboard load time under concurrent user load | Load Test | Apache JMeter / k6 | 1. Configure 200 simultaneous virtual users. 2. Each user authenticates and navigates to the Zone Dashboard. 3. Measure time from HTTP request to full page render (Time to Interactive). 4. Run for 5 minutes sustained. | 95th percentile response time is ≤ 2 seconds. No HTTP 5xx errors occur. Server CPU usage remains below 80%. | — | — |
| TC-NFR-P02 | NFR-S02 | Meter reading ingestion throughput at 500 readings/second | Stress Test | k6 / Locust | 1. Configure a load generator to submit POST requests to `/api/meters/readings` at a rate of 500 requests per second. 2. Run for 60 seconds. 3. Monitor API response codes, database write success rate, and queue depth. | All readings are persisted without data loss. No HTTP 5xx errors. Average response time for ingestion endpoint remains ≤ 500ms. Database write success rate is 100%. | — | — |

---
 
### 2.2 Security Test Cases
 
| Test Case ID | Requirement ID | Description | Test Type | Steps | Expected Result | Actual Result | Status |
|---|---|---|---|---|---|---|---|---|
| TC-NFR-S01 | NFR-SEC01 | All API communication uses TLS; HTTP redirects to HTTPS | Security Test | 1. Make an HTTP (not HTTPS) request to `http://<server>/api/dashboard`. 2. Inspect the response. 3. Inspect TLS certificate validity and version using `openssl s_client`. | The HTTP request is redirected to HTTPS (HTTP 301). The TLS version is 1.2 or higher. No API data is returned over an unencrypted connection. | — | — |
| TC-NFR-S02 | NFR-SEC03 | Rate limiting enforces max 10 login attempts per minute per IP | Security Test | 1. From a single IP address, submit 12 POST requests to `/api/auth/login` within 60 seconds with arbitrary credentials. 2. Inspect response codes for requests 11 and 12. | Requests 1–10 return HTTP 401 (invalid credentials). Requests 11 and 12 return HTTP 429 Too Many Requests with a `Retry-After` header. The IP is blocked from further attempts for the remainder of the minute. | — | — |
 
---